import { BackgroundComponent } from "../../components/Background/style";
import { CreateAccountComponent } from "../../components/CardCreateAccount";

export const CreateAccountPage = () => {
    return (
        <BackgroundComponent>
            <CreateAccountComponent/>
        </BackgroundComponent>
    )
}